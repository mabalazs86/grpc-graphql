type Customer {
  id: Int!
  name: String!
}

input CreateCustomerInput {
  name: String!
}

type Query {
  customer(id: Int!): Customer
  customers(ids: [Int!]!): [Customer]
}

type Mutation {
  createCustomer(input: CreateCustomerInput!): Customer
}
